import { styled } from "@mui/system";

export const RegisterAppContainer = styled("div")({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  padding: 16,
  fontFamily: "Roboto, Arial, sans-serif",
});

export const RegisterAppForm = styled("form")({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  width: "100%",
  maxWidth: "400px",
});
